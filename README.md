# First-time-14-8-26
**Alumno:** Gabriel Duarte Ortiz  
**Email:** gabrielduarteortiz07@gmail.com  

---

# Nota por Profesor Axel:

Hola Gabriel! Buen avance resolviendo los calentamientos 1 a 6. Reorganizé la estructura de carpetas de tu repositorio creando `clase-1/` (con `cuadrado.html`) y `clase-3/` (con tus archivos de JS).

**Puntos a tener muy en cuenta para la práctica:**
1. En `calentamiento3.js` tenías un error conceptual importante: el operador `^` en JavaScript NO eleva al cuadrado (es una operación Bitwise XOR). Para potencia debés usar `**` (ej. `altura ** 2`) o `Math.pow()`.
2. Las funciones deben retornar el valor (`return resultado`) en lugar de hacer `return console.log()`, ya que `console.log` devuelve `undefined`.
3. Tenías los ejercicios dentro de una carpeta llamada `java/`. Recordá que el lenguaje es **JavaScript** (JS) y los reubicamos en `clase-3/`.
4. **Uso de `console.log` dentro de funciones:** Tené mucho cuidado al resolver los ejercicios. Si el enunciado no pide expresamente mostrar algo en consola o en pantalla, **no se debería de consologuear nada dentro de la función**, a no ser que sea a modo de depurar un desarrollo. La función debe limitarse a computar y retornar el valor pedido.

**Estado de entrega Clase 03:**
- ✅ Calentamientos 1 a 6 completados (con correcciones).
- ⏳ Pendiente: Calentamientos 7-8, Ejercicios 9 a 13 e integración `peaje.js`.

Seguí practicando y cualquier duda me consultás en la próxima clase!
